import React from 'react';
import Modal from 'external-package-example';

export const TestComponent: React.FC = () => {
    return (
        <div>
            test 123
            <Modal />
        </div>
    );
}